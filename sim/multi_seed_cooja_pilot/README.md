# Cooja pilot bundle (5 seeds × 2 channels)

Generated by:

```bash
cd simulations/real
SEEDS="20260501 20260502 20260503 20260504 20260505" \
  SIM_TIMEOUT_MS=90000 \
  SIMDIR=../../Section-tex/sim/multi_seed_cooja_pilot \
  LOGDIR=logs/multi_seed_pilot \
  ./run_multi_seed_campaign.sh
```

**Purpose:** replace hybrid augmentation with **measured** Cooja rows for five seeds before scaling to $N{=}20$ at `SIM_TIMEOUT_MS=1800000`.

**Do not** merge into `sim/multi_seed/` until reviewed; compare `stats/summary_table.csv` against the hybrid bundle.
